name: "Release (anylinux amd64 + manylinux2014 amd64 + macOS arm64)"

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      draft:
        description: Publish a draft release and leave the `latest` tag alone.
        type: boolean
        default: false

concurrency:
  # Behavior:
  # - Group all pull requests: latest push cancels previous ones
  # - Group all branches: run serially
  group: >
    ${{
      (github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number)) ||
      ((github.event_name == 'push' || github.event_name == 'workflow_dispatch') && format('branch-{0}', github.ref_name)) ||
      ''
    }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  version:
    runs-on: ubuntu-24.04
    outputs:
      version: '${{ steps.info.outputs.version }}'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - id: info
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          BASE_VER=$(git show HEAD --format="%cs" | tr -d '\n' | tr '-' '.')
          COUNTER=0
          CURRENT_VER=$BASE_VER
          while gh release view "v$CURRENT_VER" > /dev/null 2>&1; do
            COUNTER=$(expr $COUNTER + 1)
            CURRENT_VER=$BASE_VER.post$COUNTER
          done
          echo "version=$CURRENT_VER" >> $GITHUB_OUTPUT
  build:
    needs: [version]
    strategy:
      matrix:
        os:
          [
            {
              name: "ubuntu",
              runner: "ubuntu-24.04",
              arch: "x86_64",
            },
            {
              name: "macos",
              runner: "macos-15",
              arch: "arm64",
            },
          ]
    name: Build and Test Binary Wheels | ${{ matrix.os.name }}
    runs-on: ${{ matrix.os.runner }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: true
      - if: ${{ matrix.os.name == 'macos' && matrix.os.arch == 'arm64' }}
        name: "[macOS/arm64] Install Python 3.8 (see: https://cibuildwheel.pypa.io/en/stable/faq/#macos-building-cpython-38-wheels-on-arm64)"
        uses: actions/setup-python@v5
        with:
          python-version: 3.8
      - name: Build wheels
        uses: pypa/cibuildwheel@v3.4.1
        with:
          # Explicit so it can't drift from the upload path below
          output-dir: wheelhouse
        env:
          CIBW_ARCHS: ${{ matrix.os.arch }}
          CIBW_BUILD_VERBOSITY: "1"
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux2014
          CIBW_ENVIRONMENT: >
            MACOSX_DEPLOYMENT_TARGET=11.0
            VCD2FST_WHEEL_VERSION=${{ needs.version.outputs.version }}
          CIBW_BEFORE_ALL: >
            bash {package}/wheel_build/before_all.sh
          # long options not available on macOS
          CIBW_TEST_COMMAND: >
            vcd2fst {package}/test/basic.vcd {package}/test/basic.fst 2>&1 | grep "6 vars, 6 aliases, last time 9"
      - uses: actions/upload-artifact@v7
        with:
          name: python-wheels-${{ matrix.os.name }}-${{ matrix.os.arch }}
          path: wheelhouse/*.whl
          if-no-files-found: error
  release:
    runs-on: ubuntu-24.04
    name: Create release
    needs: [version, build]
    if: github.event_name != 'pull_request'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Download all wheel artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: python-wheels-*
          merge-multiple: true
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: '${{ needs.version.outputs.version }}'
        run: |
          prerelease_arg=()
          if [ -z "${{ inputs.draft || '' }}" ]; then
            prerelease_arg+=( "--prerelease" )
          fi
          gh release create "v$TAG" dist/*.whl \
            "${prerelease_arg[@]}" \
            --repo '${{ github.repository }}' \
            --target '${{ github.sha }}' \
            --generate-notes \
            --notes 'Automated build based on commit ${{ github.sha }}'
      - name: Upload Wheels to Silimate Download Server
        if: needs.prepare.outputs.cibw_build == '*' # do NOT upload partial releases
        env:
          SERVER_SSH_KEY: ${{ secrets.SILIMATE_SERVER_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p $PWD/ssh
          echo "$SERVER_SSH_KEY" > $PWD/ssh/key
          chmod 600 $PWD/ssh/key
          SSH_OPTS="-o StrictHostKeyChecking=no -o ConnectTimeout=15 -i $PWD/ssh/key"
          rsync --verbose --rsh="ssh $SSH_OPTS" --recursive $PWD/dist/\
            ubuntu@downloads.silimate.com:/usr/share/nginx/pypi/invio/
